import './style.css';
import turnaround from '../../../assets/brand/mascot/reference/cdawg-b1-turnaround-v001.png';
import expressions from '../../../assets/brand/mascot/reference/cdawg-b1-expressions-v001.png';
const assets=import.meta.glob('../../../assets/brand/mascot/correction-v004/previews/*.png',{eager:true,query:'?url',import:'default'});
const url=name=>assets[`../../../assets/brand/mascot/correction-v004/previews/${name}.png`];
const specimen=(name,title,caption='',className='')=>`<figure class="${className}"><div class="stage"><img src="${url(name)}" alt="${title}"/></div><figcaption><strong>${title}</strong><p>${caption}</p></figcaption></figure>`;
document.querySelector('#review').innerHTML=`<header><span class="eyebrow">CDAWG / FOCUSED MODEL CHECKPOINT / V004</span><h1>A brighter, more confident CDAWG.</h1><p>The brand system and lobby direction are approved. This review covers only the corrected mascot head, expressions, leather collar and circular C tag.</p><div class="notice">Actual Blender renders · preserved quadruped rig · static specimens only · production unchanged</div><nav><a href="#head">Head & collar</a><a href="#scale">At display size</a><a href="#expressions">Six expressions</a><a href="#reference">Binding references</a></nav></header><section id="head"><h2>01 / Head, muzzle and collar</h2><p>Fixed cameras and identical area lights across corrected and earlier-model comparisons.</p><div class="grid two">${specimen('front-head','Corrected · front','Aligned brown eyes, alert lids, rounded muzzle and a flat stitched collar.')}${specimen('three-quarter-head','Corrected · three-quarter','Square silhouette, folded ears, attached brows and readable C tag.')}</div><details><summary>Compare the earlier model under the same cameras and lighting</summary><div class="grid two">${specimen('before-front-head','Earlier · front')}${specimen('before-three-quarter-head','Earlier · three-quarter')}</div></details></section><section id="scale"><h2>02 / The sizes that matter</h2><div class="grid two">${specimen('lobby-host','Lobby host · 220px character height','Default expression, replacing the earlier asymmetric sleepy host pose.','lobby')}${specimen('gameplay','Gameplay specimen · 256px source canvas','Static readability only. Existing gameplay atlases have not been regenerated.','gameplay')}</div></section><section id="expressions"><h2>03 / One character, six expressions</h2><p>All six use the same front camera, framing, lights and render settings.</p><div class="grid three">${['default','delighted','determined','worried','panic','mischief'].map(n=>specimen('expr-'+n,n[0].toUpperCase()+n.slice(1))).join('')}</div></section><section id="reference"><h2>04 / Binding canonical references</h2><p>Original supplied sheets, unaltered and reference-only. Their baked lighting is not a recoverable 3D lighting setup; the model comparisons above use a fixed, reproducible studio rig.</p><img class="reference" src="${turnaround}" alt="Approved original canonical turnaround sheet"/><img class="reference expressions" src="${expressions}" alt="Approved original canonical expression sheet"/></section><footer><strong>Static correction approved.</strong><p>The owner approved this corrected head, all six expressions, collar and tag. Animation integration is now in pre-merge validation; no merge or deployment is authorized.</p></footer>`;
